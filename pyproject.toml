[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hotel-pl-normalizer"
version = "2.0.0"
description = "Map hotel P&L workbooks into the Standard COA from the command line."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.0",
    "openpyxl>=3.1",
    "pdfplumber>=0.11,<0.12",
    "pypdf>=6.0,<7",
    "pydantic>=2.0",
    "xlrd>=2.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "reportlab>=4.0"]

[project.scripts]
hotel-pl-normalizer = "hotel_pl_normalizer.cli:main"

[tool.setuptools.packages.find]
include = ["hotel_pl_normalizer*"]

[tool.setuptools.package-data]
"hotel_pl_normalizer.prompts" = ["*.md"]
"hotel_pl_normalizer.data" = ["*.csv", "*.xlsx"]

[tool.pytest.ini_options]
testpaths = ["tests"]
